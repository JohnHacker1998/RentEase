const PropertyType = {
  APARTMENT: 'APARTMENT',
  HOUSE: 'HOUSE',
  CONDO: 'CONDO',
  STUDIO: 'STUDIO',
};

const PROPERTY_TYPES = Object.values(PropertyType);

module.exports = { PropertyType, PROPERTY_TYPES };
